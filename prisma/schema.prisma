generator client {
  provider = "prisma-client-js"
}

// データソースの定義
datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// データベースのモデル定義
model User {
  id    Int     @id @default(autoincrement())
  name  String
  email String  @unique
}